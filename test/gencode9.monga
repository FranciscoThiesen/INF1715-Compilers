#Victor Nogueira - 1511043 & Francisco Thiesen - 1611854

main() : int {
	a : bool;
    b : bool;
    c : int;
    a = false;
    b = true;
    c = 13;
    @ ( a );
	@ ( !a );
    @ ( !( a || b ) );
    @ ( !( a && !b) );
    @ ( !!a );
    @ ( !!!!a && (b || !b) );
    @ ( -c );
    return 13;
}
