#Victor Nogueira - 1511043 & Francisco Thiesen - 1611854

main() : int {
	a : bool;
	b : bool;

	a = false;
	b = true;

	@ ( a && b );
	@ ( a && a );
	@ ( b && b );
	@ ( a || b );
	@ ( a || a );
	@ ( b || b );

	return 19;
}
