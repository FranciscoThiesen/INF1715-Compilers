#Victor Nogueira - 1511043 & Francisco Thiesen - 1611854

#Testando cast de float pra bool e testando todos os operadores logicos
main() : int {
	a : float;
	b : float;
	a = 0;
	b = 1;
	@ ( a && b );
	@ ( a && a );
	@ ( b && b );
	@ ( a || b );
	@ ( a || a );
	@ ( b || b );
	return 19;
}